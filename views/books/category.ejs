<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book.category %> Books</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Books in <%= book.category %> Category</h1>

  <div class="book-list">
    <% if (books.length > 0) { %>
      <% books.forEach(book => { %>
        <div class="book">
          <p>Category: <%= book.category %></p>
        </div>
      <% }) %>
    <% } else { %>
      <p>No books found in this category.</p>
    <% } %>
  </div>
</body>
</html>
